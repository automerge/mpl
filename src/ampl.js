import Store from './ampl/store'
import Tesseract from 'tesseract'
import config from './ampl/config'

console.log("Peter's AMPL")

const aMPL = { 
  Store: Store,
  Tesseract: Tesseract,
  config: config
}

export default aMPL
